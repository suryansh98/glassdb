{
  "name": "glassdb-xray",
  "private": true,
  "version": "0.1.0",
  "description": "X-ray visualizer for glassdb — watch pages, B+trees, the WAL, and the buffer pool work in real time.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.9",
    "vitest": "^4.0.5"
  }
}
